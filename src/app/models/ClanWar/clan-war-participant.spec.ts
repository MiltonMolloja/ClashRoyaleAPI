import { ClanWarParticipant } from './clan-war-participant';

describe('ClanWarParticipant', () => {
  it('should create an instance', () => {
    expect(new ClanWarParticipant()).toBeTruthy();
  });
});
